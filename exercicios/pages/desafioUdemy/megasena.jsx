import { useState } from "react";

export default function megasena() {
    const [qtde, setQtde] = useState(mega())
    const [numbers, setNumbers] = useState(mega())

    function renderNumbers() {
        return numbers.map
    }
}