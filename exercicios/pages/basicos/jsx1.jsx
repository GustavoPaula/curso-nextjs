/* JSX É uma extensão que permite criar o HTML dentro de um código JS, todo o código vai ser convertido em JS puro,
ou seja, quem vai fazer o processo de tradução é o React, ele que vai manipular a DOM(página) e criar a página dinâmicamente para que o browser possa entender
e redenrizar a página.
*/
export default function jsx() {
    return (
        <div>
            <h1>JSX #01</h1>
        </div>
    )
}